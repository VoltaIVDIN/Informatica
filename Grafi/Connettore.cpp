#include <iostream>
#include "Connettore.h"

bool Connettore::LeggiGrafoCon( list<string> & grafo )
{
     string s;
     while( (cin >> s) != 0 )
     {
//           LeggiSubGrafo( s );                         
     }
     
     return false;
}
